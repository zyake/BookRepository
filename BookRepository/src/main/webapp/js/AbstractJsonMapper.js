/**
 * imports XhrError.js
 */

function AbstractJsonMapper() {
}

/**
 * JSONからHTML要素を生成し、ターゲットのHTML要素に追加する。
 * 要素のマッピングに失敗した場合は、XhrErrorをスローする。
 */
AbstractJsonMapper.prototype.mapToElement = function(json, addTo) {
}